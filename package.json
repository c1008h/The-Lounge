{
  "name": "chat-room",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "20.x",
    "npm":"10.x"
  },
  "scripts": {
    "build": "cd client && npm install --production=false && npm run build && npm ci",
    "start": "cd client && npm run start",
    "dev:next": "cd client && npm run dev",
    "dev": "npm-run-all --parallel dev:next dev:server",
    "electron-dev": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "electron": "29.1.5"
  },
  "description": ""
}
